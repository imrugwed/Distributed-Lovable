spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/imrugwed/lovable-config-server
      username: imrugwed
      password: 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P
      default-label: main

server.port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false

---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/imrugwed/lovable-config-server
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false